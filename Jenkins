pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'sudo apt update'
                sh 'sudo apt install nodejs'
                sh 'sudo apt install npm'
                
            }
        }
    }
}
